
let initialState = {
    firend: [
        {id: 1, name : 'Vanya'},
        {id: 2, name : 'Yan'},
        {id: 3, name : 'Jenya'},
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;

}

export default sidebarReducer;